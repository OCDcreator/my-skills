# Review target

Expected with-skill behavior:

- Recognize that generic `.transcript-flow img` rules can shrink the SVG.
- Use `.concept-map-sheet` and `.concept-map-front-image` CSS with `210mm` dimensions and `max-width: none !important`.
- Force subsequent lecture headings to a new page/sheet.
- Verify with real HTML/PDF/screenshot coverage checks.
