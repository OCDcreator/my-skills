# Review target

Expected with-skill behavior:

- Produce a deterministic A4 portrait SVG-cover workflow.
- Use `210mm x 297mm`, `viewBox="0 0 210 297"`.
- Keep a Novak/tree layout, not a circular mind map.
- Include inline SVG attributes, glyph safety, layout spacing, rendered validation, and full-page HTML/PDF insertion CSS.

This placeholder lets the static review viewer present the eval case before full agent benchmark runs.
