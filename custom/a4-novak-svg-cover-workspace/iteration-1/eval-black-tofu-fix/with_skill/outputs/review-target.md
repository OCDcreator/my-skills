# Review target

Expected with-skill behavior:

- Diagnose black blocks as missing inline presentation attributes or CSS/class dependency.
- Fix paths/hairlines with `fill="none"`.
- Replace `⇄`, `→`, `′`, `₀`, `₁`, `₂`, `·`, `–` with stable text such as `对应`, `到`, `f'(x0)`, `x0/x1/x2`.
- Add validator checks for forbidden glyphs and `<style>` dependency.
