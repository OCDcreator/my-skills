{
  "name": "open-plugin-view",
  "description": "Resolve the best plugin surface-open strategy from declared metadata, commands/view types, and selector heuristics before allowing the UI to settle.",
  "steps": [
    {
      "type": "surface-open"
    },
    {
      "type": "sleep",
      "ms": "${sleepAfterMs}"
    }
  ]
}
